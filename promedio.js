//El programa pedira 5 notas y te va a devoler el promedio de las 5 notas
var acum = 0;
for(var index = 0; index <= 5; index++){
    var nota = Number(prompt(`Ingresa la nota: ${index}`));
    acum += nota;
}

while (nota * prompt){
    alert (promtp = confirm)
}

var continuar = confirm('Desea continuar con más promedios?')
    if (continuar) {
        var nota = Number(prompt(`Ingresa la nota: ${index}`));
    } else {
        (cancelar)
    }


var promedio = acum / 5
console.log(`Promedio de notas es: ${promedio}`);